const fs = require("fs"); let c = fs.readFileSync("app/admin/follow-mails/new/[userId]/page.tsx", "utf8"); let old = c.indexOf("テンプレート</label>"); if(old > -1){ c = c.slice(0, old) + "テンプレート</label><a href=\"/admin/mail-templates\" className=\"text-xs text-teal-600 hover:underline\">テンプレート管理</a>" + c.slice(old + "テンプレート</label>".length); fs.writeFileSync("app/admin/follow-mails/new/[userId]/page.tsx", c); console.log("done"); } else { console.log("not found"); }
